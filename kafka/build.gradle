group = rootProject.group
version = rootProject.version

ext {
    kafkaVersion = '1.0.1'
    confluentVersion = '4.0.1'
    //
    baseKafkaVersion = kafkaVersion.split("\\.")[0] + '.' + kafkaVersion.split("\\.")[1]
}